{
  "scripts": {
    "build": "npm --prefix server install && npm --prefix client install --legacy-peer-deps && npm --prefix client run build",
    "start": "node server/index.js"
  },
  "devDependencies": {
    "vite": "^7.3.1",
    "vite-plugin-pwa": "^1.2.0"
  }
}
